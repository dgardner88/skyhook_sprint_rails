<h1>Listing users</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Longitude</th>
    <th>Latitude</th>
    <th>Points</th>
    <th>Dealstatus</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.longitude %></td>
    <td><%= user.latitude %></td>
    <td><%= user.points %></td>
    <td><%= user.dealStatus %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
